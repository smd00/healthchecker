# 10 21 * * * /bin/bash -l -c 'SMDHC_SOURCE=$HOME/smdhc && $SMDHC_SOURCE/health-check.sh >> $SMDHC_OUTPUT_FOLDER_PATH/health-cron.log'
*/1 * * * * /bin/bash -l -c 'SMDHC_SOURCE=$HOME/smdhc && $SMDHC_SOURCE/health-check.sh >> $SMDHC_OUTPUT_FOLDER_PATH/health-cron.log'
# Empty line makes cron valid